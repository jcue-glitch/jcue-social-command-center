# YouTube OAuth Files

Save the OAuth client JSON downloaded from Google Cloud here as:

`oauth_client.json`

The local bridge will create these files after connection:

- `token.json`
- `oauth_state.json`
- `latest_metrics.json`

These files can contain secrets or private channel analytics and are ignored by `.gitignore`.
